export enum UserType {
    USER='USER',
    ADMIN='ADMIN'
}

export enum PostType {
    REQUEST='REQUEST',
    DONATE='DONATE'
}

export enum PostInterestStatus {
    PENDING = 'PENDING',
    ACCEPTED = 'ACCEPTED',
    CLOSED = 'CLOSED'
}

export enum PostStatus {
    ACTIVE='ACTIVE',
    CLOSED='CLOSED',
    DELETED='DELETED'
}

export enum ErrorType {
    FORM_VALIDATION='Form validation error'
}